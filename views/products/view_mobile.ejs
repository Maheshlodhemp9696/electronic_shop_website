<%- include('../admin/navbar.ejs') %>
  <style>
    .slider-img{
      height:180px;
      object-fit:cover;
      transition:0.4s;
    }
    .card:hover .slider-img{
      transform:scale(1.05);
    }
  </style>
<div class="container mt-4">

  <h3 class="mb-4 text-center">Mobiles</h3>

  <div class="row g-4">
    <% mobiles.forEach(function(mobiles){ %>
      <div class="col-lg-4 col-md-8 col-sm-6">
        <div class="card shadow-sm h-100">
          <img src="/uploads/<%= mobiles.image %>" class="card-img-top slider-img">
          <div class="card-body text-center">
            <p class="small text-muted mb-2">
              <%= mobiles.image %>
            </p>
            <h5 class="small text-dark mb-2">
              <%= mobiles.mobile_name %>
            </h5>
            <h5 class="small text-dark mb-2">
              <%= mobiles.mobile_model %>
            </h5>
            <h5 class="small text-dark mb-2">
              <%= mobiles.price %>
            </h5>
            <a href="/admin/products/delete_mobile/<%= mobiles.id %>"  class="btn btn-sm btn-danger">
             🗑  Delete
            </a>
             <a href="/admin/products/update_mobile/<%= mobiles.id %>"  class="btn btn-sm btn-warning m-2">
             ✏️  Update mobiles
            </a>
          </div>    
        </div>
      </div>
    <% }) %>
  </div>
</div>
<%- include('../admin/footer.ejs') %>
