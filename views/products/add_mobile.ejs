<%- include('../admin/navbar') %>


<main class="content"> 
<h4 class="text-center">Add Mobile Product</h4>
    <div class="cards">
      <div class="card">
        <form action="/admin/products/save_mobile" method="POST" enctype="multipart/form-data" class="p-4">
          <!-- Mobile Name -->
          <div class="mb-3">
            <label class="form-label fw-semibold">Mobile Name</label>
            <input 
              type="text" 
              name="mobile_name" 
              class="form-control"
              placeholder="Enter mobile name"
              required>
            </div>
            <!-- Mobile Description -->
            <div class="mb-3">
              <label class="form-label fw-semibold">Mobile model</label>
                <input 
                    type="text"
                    name="mobile_model" 
                    class="form-control" 
                    rows="4"
                    placeholder="Write short description about mobile"
                    required>
            </div>
            <!-- Mobile Price -->
            <div class="mb-3">
              <label class="form-label fw-semibold">Mobile Price</label>
              <input
                type="number" 
                name="price" 
                class="form-control"
                placeholder="Enter mobile price"
                required>
            </div>
            <!-- Mobile Image -->
            <div class="mb-4">
              <label class="form-label fw-semibold">Mobile Image</label>
              <input    
                type="file" 
                name="image" 
                class="form-control"
                accept="image/*"
                onchange="previewMobileImage(event)"
                required>   
                <div class="mt-3 text-center">
                    <img id="imagePreview" class="preview-img">
                </div>
            </div>
            <!-- Buttons -->    
            <div class="text-center">
              <button type="submit" class="btn btn-success px-4">
                ➕ Add Mobile
              </button>
              <a href="/admin/products" class="btn btn-secondary px-4 ms-2">
                ⬅ Back
              </a>  
            </div>
        </form>
        </div>
    </div>
    <script>
      function previewMobileImage(event){
        var reader = new FileReader();
        reader.onload = function(){
          var output = document.getElementById('imagePreview');
          output.src = reader.result;
        }
        reader.readAsDataURL(event.target.files[0]);
      }
    </script>
</main>